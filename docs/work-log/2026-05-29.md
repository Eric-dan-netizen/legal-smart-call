# 2026-05-29 工作文档

## 今日完成

### 1. 批量导入客户（前端）
- Customers.vue 新增批量导入对话框
- 支持粘贴 CSV 文本 + 上传 CSV 文件两种方式
- 格式：`姓名,电话,案件类型,来源`
- 解析预览 → 调用 `POST /api/customers/batch` → 逐行显示成功/失败

### 2. TTS 联调测试脚本
- `scripts/test-tts.ts`：独立测试阿里云 NLS TTS，HMAC-SHA1 签名获取 Token
- 测试 3 种文本长度（短/中/长），延迟 < 600ms
- 输出 MP3 到 `data/tts-output/`

### 3. 数据统计模块（后端 + 前端）
- 新建 `StatisticsModule`：CallLog/CallTask/Customer 聚合查询
- `GET /api/statistics/overview` + `GET /api/statistics/trend?days=7`
- Statistics.vue 完全重写：3 张概览卡片 + 7 天外呼趋势双柱图
- 修复 TypeORM `Between` 日期范围查询

### 4. 前端多处 Bug 修复
- Login.vue：token 读取路径 `res.data.access_token` → `res.data.data.accessToken`
- Dashboard.vue：API 返回 `[data, count]` 嵌套数组解析
- Tasks.vue：同样的嵌套数组修复
- Router：新增 `beforeEach` 登录守卫

### 5. 通话录音永久存储
- CallLog 新增 `localRecordingPath` 字段
- 新建 `RecordingService`：阿里云临时 URL 下载 → 存本地 `data/recordings/`
- Demo 通话自动生成 TTS 音频并拼接保存为 MP3
- 同时保存对话转写 JSON
- `GET /api/calls/recording/:callId/file`：HTTP Range 流式播放

### 6. 通话记录页（前端）
- 新建 CallLogs.vue：列表 + 详情弹窗
- 客户信息关联查询、状态/意向筛选、关键词搜索
- 录音回放：axios blob 获取 → HTML5 `<audio>` 播放
- 对话转写：聊天气泡样式（客户左/AI 右）
- 路由注册 + 侧边栏菜单

### 7. 模拟通话跳过合规检查
- 模拟模式（无 ALIYUN_CALL_NUMBER）移到黑名单/频率/时段检查之前
- 修复 CallLog tenantId 列为空的问题

## Git 记录
```
bd1d728 fix: move mock call path before frequency/time checks, fix tenantId in simulateCall
a6e7238 feat: call logs page with recording playback + transcript view
f3ca5cc feat: permanent recording storage — download from Aliyun + local file serve
924d65d feat: TTS test script, statistics module, frontend batch import + bug fixes
```

## 测试状态
- 后端 TypeScript 编译：零错误
- 前端 Vite 构建：通过
- Jest 测试：49/49 全部通过
- 端到端验证：Demo 通话 → 录音保存 → 列表查询 → 录音播放 全链路通过
